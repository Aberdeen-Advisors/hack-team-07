/* Transcript bodies — generated from data/inbox/transcripts/*.md by scripts/store.md.
   Loaded as a classic script because fetch() is blocked on file://. The app opens these
   in place and highlights the cited line, so a citation lands on the sentence itself. */
window.BRIDGE_TRANSCRIPTS = {};
